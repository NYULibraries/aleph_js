machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t nyulibraries/aleph_js .

test:
  override:
    - docker run nyulibraries/aleph_js testem-launchers
    - docker run nyulibraries/aleph_js
